# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

BBFILE_COLLECTIONS += "asus-layer"
BBFILE_PATTERN_asus-layer = "^${LAYERDIR}/"
LAYERSERIES_COMPAT_asus-layer = "wrynose whinlatter"
